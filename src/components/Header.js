import React from 'react';
import { Link } from 'gatsby';

import Container from 'components/Container';

const Header = () => {
  return (
    <header>
      <Container type="content">
        <p>42-Zed Covid Tracker</p>
      </Container>
    </header>
  );
};

export default Header;
